# Keyring Studio — 서비스 기획서

> 나만의 이야기를 담은 아크릴 키링 쇼핑 앱

---

## 1. 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 서비스명 | **Keyring Studio** |
| 한 줄 소개 | 귀여운 캐릭터 아크릴 키링을 탐색하고 구매하는 모바일 커머스 앱 |
| 타겟 | 10~30대 여성 중심, 캐릭터 굿즈·소품 소비층 |
| 플랫폼 | 모바일 웹 (iOS/Android 브라우저 최적화, 추후 PWA/네이티브 확장) |
| 핵심 가치 | ① 감성적인 일러스트 중심의 상품 경험 ② 빠르고 단순한 구매 흐름 ③ 위시리스트 기반 재방문 유도 |

## 2. 목표

- **1차 (MVP)**: 온보딩 → 홈 탐색 → 상품 상세 → 옵션 선택 → 장바구니/구매 흐름이 완결되는 프론트엔드 구현
- **2차**: 백엔드 연동(상품·주문 API), 결제(PG) 연동, 회원 인증
- **3차**: 커스텀 키링 제작 기능(도안 업로드), 리뷰/알림 등 리텐션 기능

## 3. 정보 구조 (IA)

```
Keyring Studio
├── 온보딩 (첫 방문 시 1회)
│   └── 3장 슬라이드 → [시작하기] → 홈
├── 홈 (탭 1)
│   ├── 검색 / 장바구니 진입
│   ├── 히어로 배너 캐러셀 (신상 컬렉션)
│   ├── 카테고리 바로가기 (5종)
│   └── 인기 상품 그리드
├── 카테고리 (탭 2)
│   └── 카테고리별 상품 리스트 + 필터 칩
├── 위시리스트 (탭 3)
│   └── 찜한 상품 관리
├── 마이페이지 (탭 4)
│   └── 주문 내역 / 설정 (MVP는 정적 화면)
├── 상품 상세
│   ├── 이미지 캐러셀
│   ├── 상품 정보 (이름·가격·설명)
│   ├── 옵션 선택 (키링 타입 / 수량)
│   └── [장바구니 담기] / [바로 구매하기]
└── 장바구니
    └── 수량 조정 / 삭제 / 합계 / 주문하기
```

## 4. 화면 정의

### 4.1 온보딩 (`/onboarding`)
- 중앙 캐릭터 일러스트 + 로고 타이포 + 서브 카피("나만의 이야기를 담은 아크릴 키링을 만들어보세요.")
- 3장 슬라이드(스와이프/자동 전환), 하단 페이지 인디케이터
- `시작하기` 블랙 필 버튼 → 홈 이동, `localStorage`에 방문 기록 저장하여 재방문 시 스킵

### 4.2 홈 (`/`)
- 상단 헤더: 로고 타이포(좌) / 검색·장바구니 아이콘(우), 장바구니 수량 뱃지
- 히어로 배너: 라벤더 배경 캐러셀, "NEW ARRIVAL — 신상 키링 컬렉션" + `구경하기` CTA
- 카테고리: 캐릭터 / 심플 / 이니셜 / 파스텔 / 시즌 한정 — 원형 아이콘 + 라벨, `전체보기` 링크
- 인기 상품: 3열 그리드 카드(일러스트 + 찜 하트 + 이름 + 가격), `전체보기` 링크
- 하단 탭바: 홈 / 카테고리 / 위시리스트 / 마이페이지

### 4.3 상품 상세 (`/product/:id`)
- 상단: 뒤로가기(좌) / 공유·찜(우) 오버레이 아이콘
- 파스텔 배경 풀블리드 이미지 캐러셀 + 인디케이터
- 상품명, 가격(원화 포맷), 2줄 설명
- 옵션 선택 카드: `키링 타입`(기본 고리/고리형/체인형 바텀시트 선택), `수량`(- / +)
- 하단 고정 CTA: `장바구니 담기` 또는 `바로 구매하기` 블랙 필 버튼

### 4.4 카테고리 (`/category`)
- 카테고리 필터 칩 + 상품 그리드(2열)

### 4.5 위시리스트 (`/wishlist`)
- 찜한 상품 리스트, 빈 상태 일러스트 + 안내 문구

### 4.6 장바구니 (`/cart`)
- 담긴 상품(옵션·수량) 리스트, 수량 조정/삭제, 총 합계, `주문하기` CTA(MVP는 토스트 처리)

### 4.7 마이페이지 (`/my`)
- 프로필 영역 + 메뉴 리스트(주문 내역, 배송지, 공지, 설정) — MVP는 정적

## 5. 디자인 시스템

### 5.1 컬러

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--color-ink` | `#191919` | 기본 텍스트, 블랙 버튼 |
| `--color-sub` | `#8A8A8E` | 보조 텍스트 |
| `--color-line` | `#EFEFF1` | 구분선, 카드 보더 |
| `--color-bg` | `#FFFFFF` | 기본 배경 |
| `--color-lavender` | `#EFEAF8` | 히어로/상품 배경 (라벤더) |
| `--color-pink` | `#FBE8EC` | 상품 배경 (핑크) |
| `--color-cream` | `#FBF6E3` | 상품 배경 (크림) |
| `--color-mint` | `#E7F4EE` | 상품 배경 (민트) |
| `--color-accent` | `#7C5CBF` | 포인트 (NEW 라벨, 링크, 찜 하트) |

### 5.2 타이포그래피
- 본문: **Pretendard Variable** (CDN), 시스템 폰트 폴백
- 로고: Pretendard Bold, letter-spacing 타이트하게
- 스케일: 24/20(타이틀) · 17(상품명) · 15(본문) · 13(캡션) · 11(라벨)

### 5.3 컴포넌트 규칙
- 라운드: 카드 20px, 버튼 14px, 칩·아이콘 원형
- 버튼: Primary = 블랙 필 + 흰 텍스트, 높이 54px
- 상품 일러스트: 인라인 SVG 캐릭터(구름·토끼·별·하트·꽃 등) — 이미지 에셋 없이 벡터로 관리
- 그림자 최소화, 보더·배경색으로 위계 표현 (컨셉 시안과 동일)

## 6. 기술 스택

| 영역 | 선택 | 이유 |
| --- | --- | --- |
| 프레임워크 | React 18 + TypeScript | 컴포넌트 재사용, 타입 안정성 |
| 빌드 | Vite 5 | 빠른 개발 서버/빌드 |
| 라우팅 | react-router-dom v6 | SPA 화면 전환 |
| 상태 | React Context + `useReducer` | MVP 규모에 적정 (장바구니·위시리스트), `localStorage` 영속화 |
| 스타일 | CSS(디자인 토큰 변수) | 의존성 최소화, 토큰 기반 일관성 |
| 데이터 | 로컬 목데이터 (`src/data`) | 추후 API 교체를 전제로 타입 분리 |

## 7. 데이터 모델

```ts
type Category = 'character' | 'simple' | 'initial' | 'pastel' | 'season'

interface Product {
  id: string
  name: string          // 예: '구름 친구 키링'
  price: number         // 원 단위
  description: string[] // 상세 설명 (줄 단위)
  category: Category
  bg: string            // 파스텔 배경 토큰
  art: ArtId            // SVG 일러스트 식별자
  isNew?: boolean
  popular?: boolean
}

interface CartItem {
  productId: string
  option: string        // 키링 타입
  qty: number
}
```

## 8. 개발 로드맵

| 단계 | 범위 | 상태 |
| --- | --- | --- |
| M1 | 프로젝트 셋업, 디자인 토큰, 공통 컴포넌트(탭바·헤더·버튼) | ✅ |
| M2 | 온보딩 / 홈 / 상품 상세 | ✅ |
| M3 | 카테고리 / 위시리스트 / 장바구니 / 마이페이지 | ✅ |
| M4 | 상태 영속화(localStorage), 토스트·바텀시트 인터랙션 | ✅ |
| M5 | 백엔드 API·결제·인증 연동 | ⬜ 후속 |
| M6 | 커스텀 키링 제작 기능 | ⬜ 후속 |

## 9. 실행 방법

```bash
npm install
npm run dev      # 개발 서버 (http://localhost:5173)
npm run build    # 프로덕션 빌드
```
