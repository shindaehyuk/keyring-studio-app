# Keyring Studio — 서비스 기획서

> 나만의 이야기를 담은 아크릴 키링 — 굿즈 홍보 & 사전예약 사이트

---

## 1. 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 서비스명 | **Keyring Studio** |
| 한 줄 소개 | 아크릴 키링 굿즈를 홍보하고 정식 오픈 전 사전예약을 받는 모바일 웹사이트 |
| 성격 | 판매(결제) 없음 — **홍보 + 사전예약 접수**가 목적 |
| 타겟 | 10~30대 캐릭터 굿즈·소품 소비층, SNS 유입 방문자 |
| 플랫폼 | 모바일 웹 (SNS 링크 공유 최적화, 데스크톱에서도 모바일 프레임 유지) |
| 핵심 가치 | ① 감성 일러스트로 굿즈 매력 전달 ② 오픈 기대감 조성(카운트다운) ③ 간단한 폼으로 예약 전환 |

## 2. 목표 & 핵심 지표

- **1차 (MVP)**: 랜딩 → 컬렉션 구경 → 관심 등록 → 사전예약 완료 퍼널의 프론트엔드 구현
- **2차**: 예약 데이터 백엔드 수집(API/스프레드시트), 오픈 알림 발송(이메일·알림톡)
- **3차**: 정식 오픈 시 커머스 전환 또는 외부 스토어(스마트스토어 등) 연결

핵심 지표(KPI): 방문 대비 **사전예약 전환율**, 관심 굿즈(하트) 등록 수, SNS 공유 유입 수

## 3. 정보 구조 (IA)

```
Keyring Studio
├── 홈 (랜딩, /)
│   ├── 히어로 배너 캐러셀 (COMING SOON / 시즌 한정 / 증정 / 커스텀)
│   ├── 오픈 카운트다운 (D-day 타이머)
│   ├── 미리 만나는 키링 친구들 (대표 굿즈 그리드)
│   ├── 사전예약 혜택 3종
│   ├── 사전예약 CTA 배너
│   └── 푸터 (문의·SNS)
├── 컬렉션 (/collection)
│   └── 카테고리 필터 칩 + 굿즈 그리드
├── 굿즈 상세 (/product/:id)
│   ├── 일러스트 캐러셀 + 공유/관심
│   ├── 예약가·소개·굿즈 정보(사이즈/재질/키링 타입)
│   └── [관심 등록] / [사전예약하기]
├── 사전예약 (/reserve)
│   ├── 이름·연락처 입력
│   ├── 관심 키링 다중 선택
│   ├── 개인정보 동의
│   └── 완료 (/reserve/done — 예약 번호·혜택 안내)
├── 관심 굿즈 (/wishlist)
└── 내 예약 (/my — 예약 내역 확인·취소)
```

## 4. 화면 정의

### 4.1 홈 / 랜딩 (`/`)
- 헤더: 로고 타이포(좌) + `사전예약` 필 버튼(우)
- 히어로 배너: 파스텔 배경 캐러셀 4종(자동 전환), CTA `사전예약하기`
- 카운트다운: `LAUNCH_DATE`까지 일/시/분/초 실시간 타이머, 오픈 후엔 "지금 정식 오픈!" 표기
- 대표 굿즈 3열 그리드(NEW 뱃지·하트), 혜택 카드 3종, 마무리 CTA, 푸터

### 4.2 컬렉션 (`/collection`)
- 필터 칩(전체/캐릭터/심플/이니셜/파스텔/시즌 한정) + 2열 굿즈 그리드
- `?c=카테고리` 쿼리로 진입 시 해당 필터 프리셋

### 4.3 굿즈 상세 (`/product/:id`)
- 뒤로가기/공유(Web Share API, 폴백은 링크 복사)/관심 하트
- 파스텔 풀블리드 일러스트 + 인디케이터
- `사전예약가` 라벨 + 가격, 2줄 소개
- 굿즈 정보 카드: 사이즈 / 재질 / 제공 키링 타입
- 하단 고정 CTA: 관심 등록(아웃라인) + `사전예약하기`(블랙) → 해당 굿즈 선택된 채 폼 진입

### 4.4 사전예약 (`/reserve` → `/reserve/done`)
- 입력: 이름, 연락처(이메일/전화), 관심 키링 다중 선택(4열 픽커), 개인정보 동의
- 검증 실패는 토스트로 안내, 성공 시 예약 번호(`KS-XXXX`) 발급
- 완료 화면: 예약 번호·선택 굿즈 요약·혜택 재안내

### 4.5 관심 굿즈 (`/wishlist`) / 내 예약 (`/my`)
- 관심 굿즈: 하트 등록 목록 + "이 친구들 사전예약하기" CTA, 빈 상태 일러스트
- 내 예약: 예약 카드(번호·날짜·굿즈 썸네일) + 예약 취소

## 5. 디자인 시스템

### 5.1 컬러

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--color-ink` | `#191919` | 기본 텍스트, 블랙 버튼 |
| `--color-sub` / `--color-faint` | `#8A8A8E` / `#C7C7CC` | 보조 텍스트 |
| `--color-line` | `#EFEFF1` | 구분선, 카드 보더 |
| `--color-lavender` | `#EFEAF8` | 히어로/CTA 배경 |
| `--color-pink` / `--color-cream` / `--color-mint` / `--color-blue` | 파스텔 4종 | 굿즈 배경 |
| `--color-accent` | `#7C5CBF` | 포인트(라벨, 하트, 선택 상태) |

### 5.2 타이포그래피
- **Pretendard Variable** (CDN) + 시스템 폰트 폴백
- 스케일: 24(랜딩 타이틀) · 21(페이지 타이틀) · 17(섹션) · 14~15(본문) · 11~13(캡션·라벨)
- 카운트다운 숫자는 `tabular-nums`로 흔들림 방지

### 5.3 컴포넌트 규칙
- 라운드: 카드 20px, 버튼 14px, 칩·필 999px
- Primary 버튼 = 블랙 필 + 흰 텍스트, 높이 54px
- 굿즈 일러스트: 이미지 에셋 없이 **인라인 SVG** 8종(구름·토끼·별·하트·데이지·리본·체리·이니셜)
- 그림자 최소화, 보더·파스텔 배경으로 위계 표현

## 6. 기술 스택

| 영역 | 선택 | 이유 |
| --- | --- | --- |
| 프레임워크 | **Next.js 15 (App Router)** + React 19 + TypeScript | 홍보 사이트 특성상 **SEO·OG 공유 미리보기**가 핵심 — 서버 렌더링 필수 |
| 메타데이터 | Next Metadata API | 페이지·굿즈별 title/description/OG 자동 생성 |
| 상태 | React Context + `localStorage` | 관심 굿즈·예약 내역 클라이언트 보관 (MVP) |
| 스타일 | CSS(디자인 토큰 변수) | 의존성 최소화, 토큰 기반 일관성 |
| 데이터 | 로컬 목데이터 (`src/data`) | 추후 CMS/API 교체 전제로 타입 분리 |

> Vite SPA로 시작했다가 홍보·사전예약 방향 확정 후 Next.js로 전환. 검색엔진·카톡/인스타 링크 미리보기 노출과 추후 예약 수집 API(Route Handler) 확장을 고려한 선택.

## 7. 데이터 모델

```ts
type CategoryId = 'character' | 'simple' | 'initial' | 'pastel' | 'season'

interface Product {
  id: string
  name: string          // 예: '구름 친구 키링'
  price: number         // 사전예약가 (원)
  description: string[]
  category: CategoryId
  bg: string            // 파스텔 배경 토큰
  art: ArtId            // SVG 일러스트 식별자
  isNew?: boolean
  popular?: boolean     // 랜딩 노출 여부
}

interface Reservation {
  id: string            // 'KS-XXXXXX' 예약 번호
  name: string
  contact: string       // 이메일 또는 전화
  productIds: string[]
  createdAt: string     // ISO
}
```

## 8. 개발 로드맵

| 단계 | 범위 | 상태 |
| --- | --- | --- |
| M1 | 디자인 토큰·일러스트 시스템·공통 컴포넌트 | ✅ |
| M2 | 랜딩(배너·카운트다운·혜택) / 컬렉션 / 굿즈 상세 | ✅ |
| M3 | 사전예약 폼·완료 / 관심 굿즈 / 내 예약 | ✅ |
| M4 | Next.js 전환, 페이지별 SEO 메타데이터 | ✅ |
| M5 | 예약 수집 백엔드(Route Handler + DB/시트), 알림 발송 | ⬜ 후속 |
| M6 | 정식 오픈 전환(커머스 연결), OG 이미지 제작 | ⬜ 후속 |

## 9. 실행 방법

```bash
npm install
npm run dev      # 개발 서버 (http://localhost:3000)
npm run build    # 프로덕션 빌드
npm start        # 프로덕션 서버
```
